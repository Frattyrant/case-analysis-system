# 项目经历 — 案件侦查数据分析系统

## 一句话概述

独立开发了一款桌面端侦查数据分析工具，通过向量化优化将海量数据筛选耗时从 30 秒降至 1 秒以内，实现航班→租车→住宿→同住的链式嫌疑人追踪。

---

## STAR 详述

### S — 背景

传统的侦查数据分析依赖人工逐表比对 Excel，数据量动辄十万级，单次航班筛选耗时超过半分钟，且多个数据源（航班、租赁、住宿、轨迹）之间完全割裂，无法形成链式追踪闭环。

### T — 目标

开发一个桌面端工具，支持多数据源导入、案发城市→嫌疑人自动定位、航班→租车→住宿→同住链式追踪，且筛选操作需在秒级内完成。

### A — 行动

1. **性能瓶颈攻关**：将航班目标省份筛选从逐行 `.apply(lambda)` 改为 `Series.map(precomputed_dict)`。对 10 万行数据，只对去重后的 50 个唯一机场 IATA 码预计算一次省份归属，然后通过 pandas 底层 C 级哈希表映射整列，将核心过滤从 30s 降至约 20ms，提速约 1500 倍。

2. **地理三层兜底**：实现 `province_of()` 方法，依次走 cpca 库解析 → 省份关键字匹配 → 城市→省份字典映射，覆盖中文名、英文名、带"市"后缀等多种输入格式，并内置缓存避免重复查询。

3. **链式侦查流程**：设计状态管理器（SlotSpec 注册模式 + AppState 按案件隔离），使航班交叉表的时间窗自动传递给租赁模块、租赁司机列表自动填充住宿查询下拉框，形成完整的数据追踪链路。

4. **UI 异步化**：将分析任务放入 QThread 后台执行，主界面通过信号槽接收结果，避免界面卡死；同时实现多标签页结果展示、分页浏览、列排序、CSV/Excel 一键导出。

### R — 结果

- 核心筛选从 **30s → 1s 以内**，整体查询体验从"等待卡顿"变为"即时响应"
- 支持 5 类数据源（航班/租赁/住宿/轨迹/机动车），覆盖 3 大侦查区域
- 链式流程减少了用户在模块间手动搬运数据的时间
- 已通过 PyInstaller 打包为独立 exe，可直接分发使用

---

## 技术栈

`Python` `PyQt6` `pandas` `SQLite` `cpca` `airportsdata` `PyInstaller`

---

## 关键收获

- 理解 Python 函数调用开销对性能的量级影响，掌握向量化替代逐行的优化思路
- 学会在第三方库不可用时设计多层兜底策略（而非直接崩溃）
- 掌握了桌面端应用的完整开发→打包→分发流程
