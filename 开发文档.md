# 开发文档

## 2026-04-29: UI与后端重构 — 三大侦查区域

### 开发需求
将主UI重组为三大侦查区域的数据链式流程: Area1链式侦查(航班→租车→住宿→同住→Gang A/B)、Area2团伙分析(住宿渗透→Gang C→综合研判A∩B∩C)、Area3车辆轨迹(模糊车牌→扩展时段轨迹筛查→真伪查询)。优化租车查询使用航班交叉表时间窗，住宿查询支持下拉框姓名↔身份证联动，新增团伙比对模块，线程通信解耦(模块返回数据→service写state→GUI填充)。

### 开发结果
- **29个测试全部通过**，无回归问题
- 新增12个State Slot，总计29个
- 新增GangComparisonAnalysis模块
- 所有模块向后兼容，无破坏性API变更

### 新增/修改的文件

| 文件 | 变更类型 |
|------|----------|
| `app/main.py` | 追加12个SlotSpec |
| `app/core/state_manager.py` | 新增`AppState.get()`和`__contains__()`方法 |
| `app/modules/flight_module.py` | 提取时间窗+存储gang_b_suspects |
| `app/modules/rental_module.py` | 新增time_window参数+driver_suspects列表 |
| `app/modules/lodging_module.py` | 双结果存储+CohabitAnalysis.use_lodging_base参数+HotelPenetration存储gang_c |
| `app/modules/trajectory_module.py` | SpatialPenetrationAnalysis存储spatial_expanded_result |
| `app/modules/vehicle_module.py` | 设置plate_verification_alert标记 |
| `app/modules/gang_comparison_module.py` | **新建** — A∩B∩C三组嫌疑人交集比对 |
| `app/services_.py` | 新增6个方法: run_cohabit_from_lodging, run_gang_comparison, get_flight_earliest_latest, get_rental_driver_suspects, get_lodging_dual_results, get_plate_verification_alert |
| `app/gui_qt.py` | 三区域布局+10个新handler+ResultTabWidget action_buttons |
| `tests/test_modules_pipeline.py` | 新增8个测试用例+更新slot注册 |

### 新增核心方法

**services_.py:**
- `run_cohabit_from_lodging()` — 基于住宿结果的同住查询→Gang A
- `run_gang_comparison()` — A∩B∩C综合研判
- `get_flight_earliest_latest()` — 获取航班交叉表时间窗
- `get_rental_driver_suspects()` — 租车司机列表供下拉框
- `get_lodging_dual_results()` — 住宿双路查询结果
- `get_plate_verification_alert()` — 假牌警告标记

**gui_qt.py (MainWindow):**
- `_run_chain_flight()` — 链式航班查询+更新时间窗标签
- `_run_chain_rental()` — 链式租车查询+填充司机下拉框
- `_on_chain_lodging_selected()` — 下拉框姓名↔身份证绑定联动
- `_run_chain_lodging_name/id()` — 链式住宿双路查询
- `_run_comprehensive()` — 综合研判A∩B∩C
- `_run_vehicle_check()` — 车辆真伪查询(带假牌警告弹窗)
- `_get_action_buttons_for()` — 结果页"下一步"按钮映射
- `_scroll_to_lodging/spatial/vehicle_check()` — 导航辅助

**ResultTabWidget:**
- 新增`action_buttons`参数 — 结果页显示"下一步"操作按钮

**AppState:**
- 新增`.get(key, default)` — 字典风格安全访问
- 新增`__contains__()` — 支持`key in state`语法

## 2026-04-30a: 删除综合研判 + 合并冗余Slot

### 开发需求
综合研判(A∩B∩C)功能过重，删除；Slot注册冗余(gang_a/b/c与已有DataFrame重复、driver_lodging_by_both与name/id结果重复)，合并精简。

### 开发结果
- 删除 `gang_comparison_module.py` 整个文件
- 删除5个冗余slot: `comprehensive_result`, `gang_a_suspects`, `gang_b_suspects`, `gang_c_suspects`, `driver_lodging_by_both`
- Slot从29精简至**24个**
- 清理所有模块中对已删除slot的写入操作
- 27个测试通过

## 2026-04-30b: 同住查询移至住宿结果页

### 开发需求
Step4独立卡片冗余 — 同住查询放入Step3司机住宿查询结果页面的操作按钮中。

### 开发结果
- 删除工作区Step 4卡片，保留 `_get_action_buttons_for()` 中的入口

## 2026-04-30c: 航班查询加速 + 轨迹结果页嵌入真伪查询

### 开发需求
航班查询~30s太慢；车辆真伪查询合并到扩展时段轨迹筛查结果页中。

### 开发结果
- `geo_utils.py`: 新增 `build_airport_province_map()` 批量预计算机场→省份映射 + `province_of()` 缓存
- `flight_module.py`: 用 `df.map(airport_map)` 向量化替换 `.apply()`，去不必要的copy，预计提速100x
- `gui_qt.py`: ResultTabWidget 新增 `bottom_widget` 参数；`_make_vehicle_check_widget()` 构建黄色警告卡片(输入框+核查按钮)嵌入轨迹结果页
- 删除 `_scroll_to_vehicle_check()` 废弃方法
- 27个测试通过

### 新增方法

**geo_utils.py:**
- `build_airport_province_map(codes, target_provinces)` — 批量构建IATA→是否目标省份的bool映射

**gui_qt.py:**
- `_make_vehicle_check_widget()` — 构建轨迹结果页内联真伪查询组件
- `_run_vehicle_check_from_traj()` — 从轨迹结果页触发真伪查询

**ResultTabWidget:**
- 新增 `bottom_widget` 参数 — 支持表格下方嵌入自定义组件

## 2026-04-29d: 航班/租车UI优化 + 轨迹查询重构为日期范围选择

### 开发需求
1. 航班结果页删除"下一步：传递时间窗给租车查询"按钮，时间窗在主界面展示即可
2. 租车查询卡片的"时间窗来源"标签拆分为两个独立标签，明确显示起租/停租时间来源
3. 车辆与轨迹侦查：删除工作台中的"扩展时段轨迹筛查"（第二步），改为在车牌模糊查询结果页底部嵌入日期范围选择器（QDateEdit起止日期+查询按钮），默认取案发首末时间

### 开发结果
- 13个测试全部通过
- `SpatialPenetrationAnalysis.run()` 签名从 `buffer_days: int` 改为 `start_date: str, end_date: str`
- 结束日期自动归一化到23:59:59，确保同日时间可匹配
- 删除 `_scroll_to_spatial()` 废弃方法

### 新增/修改的方法

**gui_qt.py:**
- `_make_trajectory_search_widget()` — 构建日期范围选择组件，嵌入车牌模糊查询结果页底部
- `_set_traj_default_dates()` — 从案发点设置轨迹查询默认起止日期
- `_run_trajectory_search()` — 从结果页触发扩展时段轨迹筛查
- `rental_window_label` 拆分为 `rental_start_label` + `rental_end_label`

**trajectory_module.py:**
- `SpatialPenetrationAnalysis.run(state, start_date='', end_date='')` — 重签名，未指定日期时回退案发点首末时间

**services_.py:**
- `run_spatial(start_date='', end_date='')` — 签名同步更新

## 2026-05-01a: UI精细化优化 + 冗余Slot清理

### 开发需求
1. 案发记录逐条删除（×按钮）替代只能全清
2. 案件新建/切换分离 → 下拉框即选即切
3. 案件ID不允许重复
4. 清理2个冗余Slot（matched_trajectories、spatial_expanded_result）
5. 航班结果三表视图重命名 + 添加来源描述
6. 删除所有Gang A/B/C显示文字
7. 表格字体缩小、×按钮居中、弹窗居中、导出路径可选、新建后清空输入框

### 开发结果
- Slot从21精简至**19个**
- 新增 `app/resources/x-mark.svg` — 删除按钮图标
- `_refresh_case_points()` 用 QListWidgetItem + setItemWidget 替代 addItem，每条记录左侧×按钮
- `_add_case_point()` 增加 RuntimeError 捕获，无案件时提示
- `_create_case()` 增加重复ID检查；新建后 `case_input.clear()`
- 删除"切换案件"按钮，`case_combo.textActivated` 自动切换
- 新增 `_refresh_main_interface()` — 切换案件时恢复画像字段 + 重置时间窗
- `_centered_msgbox()` 统一弹窗居中（warning/info/critical/question）
- `_export_current/export_all` 改用 QFileDialog 让用户选择保存位置
- ResultTabWidget 新增 `descriptions` 参数 + `desc_label`，航班三表各显示来源提示

### 新增方法

**services_.py:**
- `get_case_point_date_range()` — 获取案发点首末日期
- `remove_case_point(index)` — 按索引删除单条案发记录

**gui_qt.py (MainWindow):**
- `_centered_msgbox()` — 创建居中于父窗口的提示框
- `_show_center_warning/critical/question()` — 居中弹窗封装
- `_create_case()` — 新建案件（原_create_or_switch_case拆分）
- `_on_case_selected()` — 下拉框即选即切
- `_refresh_main_interface()` — 切换案件后刷新画像+时间窗
- `_remove_case_point(index)` — 删除单条案发记录
- `_refresh_case_points()` — 重写为自定义widget（×按钮+文本）

**ResultTabWidget:**
- 新增 `descriptions` 参数 — 表名→描述文字映射
- 新增 `desc_label` — 表格上方蓝色提示条
- `_switch_table()` 联动更新描述文字

## 2026-04-30f: 删除主界面Step3司机住宿查询 + 下拉框默认空白

### 开发需求
主界面左侧链式侦查流程中Step3司机住宿查询与Step2租车结果页内嵌的 `_make_lodging_query_widget()` 功能重复，删除主界面冗余卡片。同时优化结果页司机下拉框，默认不选中任何司机（设为-1），用户需手动点击选择。

### 开发结果
- 删除 `_build_workspace()` 中 Step3 司机住宿查询 GroupBox（约25行）
- `_run_chain_rental()` 简化为直接调用 `self._services.run_rental`，移除对已删除控件 `chain_lodging_combo` 的引用
- `_populate_result_lodging_combo()` 中 `setCurrentIndex(0)` → `setCurrentIndex(-1)`
- 下拉框默认显示 placeholder 文本"选择司机嫌疑人"，姓名/身份证字段保持空白

### 遗留死代码
以下三个方法依赖已删除的UI控件，暂不删除：
- `_on_chain_lodging_selected()` 
- `_run_chain_lodging_name()`
- `_run_chain_lodging_id()`

## 2026-05-01b: 导出支持xls/xlsx + PyInstaller打包 + 路径兼容修复

### 开发需求
1. 导出功能增加 Excel (.xlsx / .xls) 格式，不只是 CSV
2. PyInstaller 打包为独立 .exe，用户无需安装 Python 环境
3. 打包后路径解析错误修复（`__file__` 在 PyInstaller 中指向临时目录，需改用 `sys.executable`）

### 开发结果
- 新增 `_detect_export_format()` — 根据文件扩展名自动选择写入引擎
- `_export_current()` 保存对话框新增 xlsx/xls 过滤器
- `_export_all()` 导出前弹出格式选择框（CSV / Excel），然后选目录批量导出
- `requirements.txt` 新增 `xlwt>=1.3`
- `config_db.py`、`data_paths.py`、`main.py` 三处 `ROOT_DIR` 增加 `sys.frozen` 检测
- PyInstaller 打包 → `dist/案件侦查数据分析系统/`（~160MB，含完整 Python 运行时）
- 首次运行自动在 exe 同级创建 `data/` 和 `logs/` 目录
- `app.spec` — PyInstaller 打包规格文件

### 新增方法

**gui_qt.py (ResultTabWidget):**
- `_detect_export_format(dest)` — 根据文件扩展名返回格式和写入参数

### 打包命令（后续修改源码后）
```bash
pyinstaller --onedir --windowed --name "案件侦查数据分析系统" \
    --add-data "app/resources/chevron-down.svg;app/resources" \
    --add-data "app/resources/chevron-up.svg;app/resources" \
    --add-data "app/resources/x-mark.svg;app/resources" \
    --exclude-module tests \
    --hidden-import openpyxl --hidden-import xlwt \
    --hidden-import cpca --hidden-import airportsdata \
    --hidden-import lxml --hidden-import sqlalchemy --hidden-import pandas \
    --clean --noconfirm \
    app/main.py
rm -rf build "案件侦查数据分析系统.spec"
```

## 2026-04-30e: 修复 QComboBox/QDateEdit 下拉箭头不显示

### 开发需求
Qt 6.11 CSS 解析器无法正确处理 `url(data:image/svg+xml;base64,...)` 中的分号，导致 QComboBox、QDateEdit、QSpinBox 的下拉箭头/调节箭头全部消失。

### 开发结果
- 新建 `app/resources/chevron-down.svg` 和 `chevron-up.svg` 图标文件
- `_apply_readable_style()` 改为 f-string 动态注入绝对路径，用 `url({path})` 替代 data URL
- 13个测试通过

### 新增文件
- `app/resources/chevron-down.svg` — 下拉箭头图标
- `app/resources/chevron-up.svg` — 上拉箭头图标（SpinBox）

## 2026-05-01c: 修复 PyInstaller 打包后 exe 无法启动

### 开发需求
双击 `案件侦查数据分析系统.exe` 无窗口/报错，排查打包问题。

### 开发结果
- 定位 3 个打包缺陷并修复
- 语法检查通过，`_resource_dir()` 开发模式工作正常
- 需重新打包验证

### 根因分析

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| 1 | `qt.conf` 缺失 | **致命** | Qt 靠 qt.conf 定位 `platforms/plugins/` 目录，缺失导致 QApplication 创建失败，exe 无窗口。PyInstaller 未自动生成此文件 |
| 2 | `__file__` 资源路径错误 | 中 | frozen 模式下 `__file__` 指向 PYZ 虚拟归档（如 `exe路径/app/gui_qt.pyc`），`os.path.dirname(__file__)` 找不到文件系统中 `_internal/app/resources/` 下的 SVG |
| 3 | `app.spec` datas 不完整 | 低 | 未包含 qt.conf，每次重新打包都会缺失 |
| 4 | `airportsdata` CSV 数据文件缺失 | **致命** | `airportsdata` 包内部读取 `airports.csv`，PyInstaller 不会自动收集第三方包的非代码数据文件 |

### 修改的文件

| 文件 | 变更类型 |
|------|----------|
| `dist/../_internal/PyQt6/Qt6/qt.conf` | **新建** — Qt 插件配置 [Paths] Prefix=. Plugins=plugins |
| `dist/../_internal/airportsdata/*.csv` | **新建** — 从 site-packages 复制 airports.csv + iata_macs.csv |
| `app/resources/qt.conf` | **新建** — qt.conf 源文件，供打包用 |
| `app.spec` | 追加 `('app/resources/qt.conf', 'PyQt6/Qt6')` + `collect_data_files('airportsdata')` |
| `app/gui_qt.py` | 新增 `_resource_dir()` + 修复 2 处资源路径 |

### 新增方法

**gui_qt.py:**
- `_resource_dir()` — 返回资源目录绝对路径，frozen 时用 `sys._MEIPASS`，开发时用 `__file__`。解决 `__file__` 在 PYZ 归档中无法映射到文件系统真实路径的问题。

**app.spec:**
- `from PyInstaller.utils.hooks import collect_data_files` — 自动收集第三方包（如 `airportsdata`）的非代码数据文件

### 后续打包命令（推荐用 spec 文件，数据收集更完整）
```bash
pyinstaller app.spec
```
或等效命令行：
```bash
pyinstaller --onedir --windowed --name "案件侦查数据分析系统" \
    --add-data "app/resources/chevron-down.svg;app/resources" \
    --add-data "app/resources/chevron-up.svg;app/resources" \
    --add-data "app/resources/x-mark.svg;app/resources" \
    --add-data "app/resources/qt.conf;PyQt6/Qt6" \
    --collect-data airportsdata \
    --exclude-module tests \
    --hidden-import openpyxl --hidden-import xlwt \
    --hidden-import cpca --hidden-import airportsdata \
    --hidden-import lxml --hidden-import sqlalchemy --hidden-import pandas \
    --clean --noconfirm \
    app/main.py
rm -rf build "案件侦查数据分析系统.spec"
```

## 2026-05-06: 查询结果新增总结栏 + 扩展时段筛查更名

### 开发需求
在每个查询结果页表格下方新增总结栏：
1. 航班嫌疑人查询 → 展示交叉表锁定的嫌疑人名字
2. 汽车租赁查询 → 展示司机名字 + 多案团伙嫌疑人
3. 司机住宿查询 → 展示被查询嫌疑人名字
4. 司机关联同住查询 → 展示同住嫌疑人名字
5. 嫌疑团伙住宿查询 → 展示去重后的锁定嫌疑人名字
6. 车辆真伪查询 → 展示车牌真伪核查结论
7. 将"扩展时段轨迹筛查"更名为"案发相邻时间段筛查"

### 开发结果
- 28个测试中27个通过（1个预存失败，与本次修改无关）
- 保留所有原有接口，无破坏性变更

### 新增/修改的文件

| 文件 | 变更类型 |
|------|----------|
| `app/gui_qt.py` | ResultTabWidget 新增 summary_text 参数 + MainWindow 新增 _build_summary_text 方法 + 更名 |
| `app/services_.py` | run_spatial() 文档字符串同步更名 |

### 新增核心方法

**ResultTabWidget:**
- 新增 `summary_label` 控件，渲染在表格下方、操作按钮上方，蓝色背景提示条样式
- `__init__` 新增 `summary_text` 参数（可选 str）

**gui_qt.py (MainWindow):**
- `_build_summary_text(title, result_df)` — 根据查询类型从 state 提取数据，生成易读的总结文字
- `_last_checked_plate` — 实例属性，记录最后一次车辆真伪查询的车牌号

### 更名映射
- `扩展时段轨迹筛查` → `案发相邻时间段筛查`（含标题、提示文字、内部 key）

### 闪退修复（同日追修）

**根因分析：**

| # | 问题 | 根因 | 修复方式 |
|---|------|------|----------|
| 1 | 未创建案件时点击任意查询按钮 → 闪退 | `_run_analysis()` 首行 `_sync_profiles_silent()` → `set_profiles()` → `current_state()` 抛出 `RuntimeError`，未被捕获 | 新增 `_ensure_active_case()` 守护方法，在 `_run_analysis` 入口处检查，无案件时弹窗警告并 return |
| 2 | 未创建案件时点击"同步"按钮 → 闪退 | 同上链路，`_on_sync_button_clicked` 直接调用 `_sync_profiles_silent()` | `_on_sync_button_clicked` 入口处增加 `_ensure_active_case()` 检查 |
| 3 | 未创建案件时点击"清空案发记录" → 闪退 | `_clear_case_points()` → `clear_case_points()` → `current_state()` 抛出 `RuntimeError` | `_clear_case_points` 入口处增加 `_ensure_active_case()` 检查 |
| 4 | 结果页表格为空时点击"导出当前表" → 闪退 | `ResultTabWidget._export_current()` 内调用 `self._show_center_warning()`，该方法仅存在于 `MainWindow`，`ResultTabWidget` 无此方法 → `AttributeError` | 替换为 `QMessageBox.warning(self, ...)` |
| 5 | 结果页表格为空时点击"一键导出全部" → 闪退 | 同上，`_export_all()` 内调用了 `self._show_center_info/warning()` | 替换为 `QMessageBox.information/warning(self, ...)` |

### 新增方法
- `MainWindow._ensure_active_case()` — 检查当前是否有活跃案件，无则弹窗警告并返回 False

### UI文案与交互微调（同日二次迭代）
1. 司机住宿查询总结 → 改为操作提示（假身份证风险提示 / 同名筛选建议）
2. 汽车租赁查询总结 → 删除司机嫌疑人列表，改为提示用户使用下方住宿查询的原因
3. 嫌疑团伙住宿查询总结 → "锁定嫌疑人-去重" → "锁定嫌疑人"
4. 车辆真伪查询 → 删除二次确认弹窗，按钮"核查真伪" → "核查车牌号真伪"
5. × 按钮 SVG → 颜色红色改黑色，图标尺寸 14→16px
6. 回滚存档功能（保存结果/读取存档）
```
