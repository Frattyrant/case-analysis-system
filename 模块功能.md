---

Area 1：链式侦查（航班 → 租车 → 住宿 → 同住）

这是串行链路，上一步的输出是下一步的输入，层层递进缩小嫌疑人范围。

---

查询 1：航班嫌疑人查询

输入条件：

- 已上传包含「航班」关键字的表（必须含列：航班日期、到达地、身份证号、姓名）
- 已录入案发记录（日期 + 城市）
- 可选：嫌疑人画像（性别 / 户籍南北），用于收窄结果

处理逻辑：

1. 从案发城市的名称，通过 GeoUtils.province_of() 反查所属省份（走 cpca 库 → 城市字典 → 省份关键字 三层兜底）
2. 用 build_airport_province_map() 批量预计算每一个航班到达地机场 IATA 码 → 是否落在目标省份（用 airportsdata 库查
   IATA → subd → 中文省份名），产出 {code: True/False} 字典
3. 向量化过滤：df['到达地'].map(airport_map)，一行代码筛出所有飞到目标省份的航班，避免逐行 .apply()，耗时从 ~30s 降到
   ~1s
4. 时间切割：
   - 表1（到达乘客）：航班日期 ≤ 首案日期，且落地省份匹配 → 再用 IdentityUtils.apply_filters() 按性别/户籍过滤
   - 表2（离开乘客）：航班日期 ≥ 末案日期（不限到达地）→ 同样过画像过滤
   - 表3（交叉嫌疑人）：表1 ∩ 表2，按 姓名 + 身份证号 做 inner
   join，既在案发前到达、又在案发后离开的人，这就是你要找的嫌疑人
5. 把表3里的最早到达时间、最晚离开时间存到 State，作为下一步租车查询的时间窗

输出结果：

- 三张表：到达乘客（案发前） / 离开乘客（案发后） / 交叉比对嫌疑人
- 每张表上方有蓝色描述条，说明数据来源
- 表格下方的蓝色总结栏显示交叉表锁定的嫌疑人姓名
- 界面上更新时间窗标签（起租/停租时间），供下一步租车查询自动引用

---

查询 2：汽车租赁查询

输入条件：

- 已上传包含「租赁」关键字的表（必须含列：起租时间、停租时间、号牌号码、租车人姓名、租车人身份证号码）
- 时间窗来源（优先级递减）：
  a. 上一步航班交叉表产出的 flight_earliest_arrival / flight_latest_departure
  b. 案发点首末日期（回退方案）
- 可选过滤：租赁公司名称（模糊匹配）、车辆品牌（精确匹配），在界面上录入

处理逻辑：

1. 时间窗覆盖判定：起租时间 ≤ 最早到达时间 AND 停租时间 ≥ 最晚离开时间，意思是这个人的租期完整覆盖了案发时间范围
2. 叠加公司名/品牌过滤（如填写）
3. 交叉轨迹数据：把确认的租赁车辆 inner join 轨迹表（按 号牌号码），筛选租赁期内有轨迹记录的记录 →
   司机姓名去重，存为「租赁轨迹嫌疑人」
4. 司机姓名+身份证号列表存到 State，下一步住宿查询的下拉框直接从这里取

输出结果：

- 一张表：租赁全库匹配正式名单
- 下方的蓝色总结栏提示用户使用下方的司机住宿查询进一步深挖
- 界面上住宿查询的下拉框自动填充司机姓名（选择后自动联动身份证号）

---

查询 3：司机住宿查询

输入条件：

- 已上传包含「旅店/住宿/旅业」关键字的表
- 从上一步租车结果下拉框选择司机（自动带入姓名 + 身份证），或手动输入
- 支持姓名和身份证号单独查，也可以联合查（OR 逻辑）

处理逻辑：

1. 根据输入构建 mask：纯姓名 → df['姓名'] == name；纯身份证 → df['身份证号'] == id；都有 → OR 合并
2. 时间列（入住时间/退房时间）用 DateTimeUtils.clean_time_format() 清洗成标准格式
3. 结果分两路存储（按姓名 / 按身份证），支持在结果页切表对比——如果姓名和身份证查出的结果不一致，说明可能存在假身份证

输出结果：

- 一张或多张住宿记录表（可在姓名/身份证结果间切换）
- 蓝色总结栏给出操作提示：若两表记录不一致 → 假身份证风险；若记录过多 → 建议用同住查询缩小范围
- 结果页有"下一步：司机关联同住"按钮

---

查询 4：司机关联人员同住查询

输入条件：

- 前提：已执行司机住宿查询（use_lodging_base=True 模式）
- 住宿数据表

处理逻辑：

1. 从住宿查询结果里提取所有不重复的嫌疑人姓名
2. 对每个嫌疑人姓名 X：
   - 在全量住宿表中找到 X 的入住记录
   - 以 X 每次入住的旅馆名称 + 入住时间 ± 24 小时为窗口，在全量住宿表中找所有同旅馆、同时间窗口的人
   - 标记 同住关系：本人 → "本人"，其他人 → "潜在同行人"
3. 每个嫌疑人的命中结果 concat + 去重（按姓名 + 身份证 + 入住时间 + 旅馆名称）

输出结果：

- 一张表，字段包含：姓名、身份证号、同住关系、入住时间_标准、旅馆名称、房号、旅馆地址
- 蓝色总结栏列出找到的同住嫌疑人名字

---

Area 2：团伙分析

查询 5：嫌疑团伙住宿渗透查询

输入条件：

- 住宿数据表
- 案发记录（案发日期 + 城市/地点关键词）
- 可选：嫌疑人画像（性别/户籍）

处理逻辑：

1. 先用画像过滤全量住宿数据
2. 对每条案发记录，以 案发日期前 1 天 ~ 案发日期当天 为窗口，在住宿表中找 旅馆地址 或 旅馆名称 包含案发地点关键词的人
3. 关键算法——交集运算：每个案发点产出一批命中身份证号，取所有案发点的身份证号交集。只有在所有案发地点都出现过的人才会
   被保留
4. 交集为空则返回空表

输出结果：

- 一张表，额外附带列：关联案发点、对应案发日期、筛选时间窗口
- 蓝色总结栏列出锁定嫌疑人（去重后的姓名）

---

Area 3：车辆与轨迹侦查

查询 6：车牌模糊查询

输入条件：

- 轨迹数据表（含 号牌号码、通过时间 列）
- 车牌号片段（如只记得 "京A" 或 "234"）

处理逻辑：

1. 把用户输入的每个字符之间插入 .*（正则通配），构建模糊匹配正则：如输入 京A123 → .*京.*A.*1.*2.*3.*
2. 在轨迹表中用 str.contains(regex) 模糊匹配号牌号码
3. 结果按通过时间降序排列
4. 匹配到的唯一车牌号存到 State，供下一步使用

输出结果：

- 一张轨迹记录表
- 蓝色总结栏列出模糊匹配到的所有唯一车牌号
- 结果页底部嵌入案发相邻时间段筛查组件（日期选择器 + 查询按钮），默认起止日期为案发首末时间

---

查询 7：车辆真伪查询

输入条件：

- 已上传机动车登记库（含「机动车」关键字）或租赁库（含「租赁」关键字）
- 完整车牌号
- 入口有两个：一是 Area 3 的独立卡片，二是嵌在车牌模糊查询结果页底部

处理逻辑：

1. 把输入的车牌号转大写
2. 同时在两个库中精确匹配 号牌号码 == plate：
   - 机动车登记库命中 → 身份结论 = "真牌 (个人/单位)"
   - 租赁库命中 → 身份结论 = "真牌 (租赁车辆)"（租车人姓名 列重命名为 所有人）
3. 两个库都命中 → concat 合并
4. 两个库都没命中 → 设置 plate_verification_alert = True，界面弹窗警告

输出结果：

- 一张核查结果表，含 备案来源、身份结论、所有人 列
- 如两个库都查不到 → 蓝色总结栏显示车牌 + "疑似假牌"，且界面弹出警告弹窗
- 如查到 → 蓝色总结栏显示 "该车牌号经核查为真牌"

---

全流程数据关系总览

上传 Excel/CSV → pandas DataFrame 存内存 (State)
↓
案发记录录入（日期 + 城市/地点）
↓
┌────────────────────┼────────────────────┐
│                    │                    │
Area 1 (链式)        Area 2 (团伙)       Area 3 (轨迹)
│                    │                    │
航班碰撞          住宿渗透交集          车牌模糊匹配
│ 时间窗              │                    │ 车牌列表
租车覆盖           锁定嫌疑人            时段轨迹筛查
│ 司机列表                               │
司机住宿                                   │ 完整车牌
│ 嫌疑人姓名                             │
同住关联                              车辆真伪核查
│                                    (双库比对)
潜在同行人

所有查询结果都支持：

- 右键复制单元格
- 导出 CSV / Excel (.xlsx / .xls)，可选择保存路径
- 一键导出所有表
- 表格之间切换查看（Tab 标签页）
