# 长期记忆

## 项目理解

- **架构**: PyQt6前端 + Python后端 + SQLite(可选MySQL)，业务模块→服务层→状态管理→GUI四层分离
- **状态管理**: AppState非单例，多案件由StateManager按case_id管理，SlotSpec注册表定义字段schema，类似"动态列的数据表"
- **通信机制**: AnalysisWorker(QThread)后台执行→finished_sig发射信号→主线程GUI更新
- **模块模式**: 所有分析模块继承AnalysisModule ABC，run方法接收AppState，返回DataFrame，同时写入state供后续模块消费

## 踩坑记录

1. **AppState没有.get()方法**: AppState的`__getattr__`将属性访问路由到`__getitem__`，调用`state.get(key, default)`会被解释为查找名为'get'的slot而报错。解决方案：为AppState显式定义`get()`实例方法，实例方法优先于`__getattr__`。

2. **Write工具要求先Read**: 编辑文件前必须先Read该文件，否则会出现"File has been modified since read"错误。覆盖场景用Write比多次Edit更高效。

3. **Python `str | None`语法**: 在旧版Python(3.9-)不支持，项目使用`from __future__ import annotations`避免类型注解求值问题。

4. **cpca.transform()是航班查询的性能杀手**: `province_of()`每行调用`cpca.transform()`逐行解析地址，10万行数据≈30s。解决方案：对唯一机场代码批量预计算→构建`{IATA: bool}`映射→用`df['列'].map()`向量化，每个唯一代码只算一次，提速100x。

5. **ResultTabWidget扩展用bottom_widget更灵活**: 比action_buttons更通用，可嵌入输入框+按钮等任意组件。轨迹结果页嵌入真伪查询即是此模式。

## 代码风格约束

- `from __future__ import annotations` 在每个.py文件顶部
- 所有注释用中文
- QGroupBox标题前有空格缩进如`(" 第一步：...")`
- QPushButton蓝色主题按钮用`setObjectName("ActionBtn")`
- DataFrame列名用中文(姓名、身份证号、号牌号码等)
- 导入顺序: 标准库 → 第三方 → 项目内部

## 常用规则

- 添加新slot必须在`main.py`的`_register_slots()`中注册，否则AppState无法读写
- 模块写state用`state["key"] = value`，读state安全用法`state.get("key", default)` (需get方法支持)
- Worker线程执行`fn()`→返回DataFrame→GUI接收并显示

## 命令记忆

- 运行测试: `python -m pytest tests/ -v`
- 检查语法: `python -c "import py_compile; py_compile.compile('file.py', doraise=True)"`
- 启动应用: `python -m app.main`
- 项目根目录: `C:\Users\LENOVO\Desktop\project`

## 踩坑记录（续）

6. **`pd.Series.between(start, end)` 同日边界问题**: 当 start == end == 某日00:00:00 时，同日08:00:00不会被between匹配。解决方案：结束日期归一化到 `+1天 -1秒` (23:59:59)。

7. **工作台控件迁移到结果页的模式**: 删除工作台中的操作卡片，改为在 `_on_analysis_finished` 中根据标题传递 `bottom_widget` 给 `ResultTabWidget`。轨迹筛查、车辆真伪查询均使用此模式。`_get_action_buttons_for()` 返回空列表即可。

8. **Qt CSS 不支持 `data:image/svg+xml;base64` 格式**: Qt 6.11 CSS 解析器将 `;base64` 中的分号解释为语句分隔符，导致 `url()` 内的 data URI 被截断，QComboBox/QDateEdit/QSpinBox 的箭头图标不显示。解决方案：将 SVG 保存为独立文件，用 `url({绝对路径})` 引用。

9. **删除主界面控件时需同步清理所有引用**: 删除UI控件的代码块后，必须检查所有方法中对该控件的引用（如 `self.chain_lodging_combo`），否则运行时会抛出 AttributeError。本次删除了Step3卡片但 `_run_chain_rental()` 还在填充 `chain_lodging_combo`，导致潜在的崩溃。

10. **QComboBox默认空白用setCurrentIndex(-1)** : 需求要求下拉框不预选任何项时，用 `setCurrentIndex(-1)` 配合 `setPlaceholderText()` 实现。设0会自动选中第一项并触发 `currentIndexChanged` 信号。

11. **QListWidget.clear() 与 setItemWidget() 交互会导致闪退**: clear() 同时删除 C++ 层 item 和 widget，同一帧内重建时旧 Python 包装器未完全释放，底层对象状态冲突。解决方案：逐项 `takeItem(0)` 移除所有权，让 Qt 在合适时机回收。

12. **QListWidgetItem 需要显式导入**: 从 `PyQt6.QtWidgets` 导入 `QListWidgetItem`，仅导入 `QListWidget` 不够。遗漏会导致 NameError 闪退。

13. **QComboBox.textActivated 仅响应用户交互**: 与 `currentTextChanged` 不同，`textActivated` 只在用户从弹出菜单选择时触发，`setCurrentText()` / `addItems()` 等程序化修改不会误触发。适用于"下拉即选即切"场景。

14. **QPushButton 内联特殊字符在 setItemWidget 场景可能不渲染**: QListWidget 中通过 `setItemWidget` 嵌入的按钮，其 CSS 文本样式（如 "×"）可能被 Widget 嵌套层级吞掉。参照下拉箭头修复模式，用 `QIcon` + SVG 文件替代文本。

15. **结果页嵌入描述的通用模式**: ResultTabWidget 新增 `descriptions` 参数（dict[str, str]）+ `desc_label` 蓝色提示条，`_switch_table()` 时联动 `self._descriptions.get(key)` 更新文字。航班三表即用此模式展示每条数据来源。

16. **QMessageBox 居中**: 通过 `msg.sizeHint()` 预计算尺寸 + `parent.geometry().center()` 计算偏移，在 `msg.exec()` 前调用 `msg.move()` 定位。

17. **PyInstaller 打包后 `__file__` 指向临时目录**: `sys.frozen` 为 True 时，`__file__` 指向 PyInstaller 解压后的 `_internal/` 临时目录，而非 exe 所在目录。所有路径解析（数据库、日志、数据目录）需统一通过 `sys.executable` 获取 exe 位置：`Path(sys.executable).parent if getattr(sys, 'frozen', False) else Path(__file__).parent.parent`。

18. **pandas 导出 Excel 引擎选择**: `.xlsx` → `engine='openpyxl'`，`.xls` → 默认 xlwt（自动），`.csv` → `encoding='utf-8-sig'`。用 `getattr(df, f'to_{fmt}')` 统一调用。

19. **PyInstaller --onedir 模式支持零配置分发**: 输出文件夹内含 exe + `_internal/`（嵌入式 Python 运行时 + 所有依赖 DLL），用户无需安装任何环境。首次运行后在 exe 同级自动创建 `data/`（SQLite DB + 上传/导出/缓存目录）+ `logs/`。

20. **PyInstaller 打包后 `qt.conf` 缺失导致 exe 无窗口**: PyInstaller 6.x + PyQt6 的 `--onedir` COLLECT 模式不一定自动生成 `qt.conf`。Qt 依赖此文件定位 `platforms/`、`styles/` 等插件目录，缺失则 `QApplication` 创建失败（无报错窗口、exe 直接退出）。解决方案：在 `app/resources/` 存放 `qt.conf` 源文件（内容 `[Paths]\nPrefix = .\nPlugins = plugins`），spec 的 datas 中添加 `('app/resources/qt.conf', 'PyQt6/Qt6')`。

21. **`__file__` 在 PyInstaller PYZ 归档中指向虚拟路径**: frozen 模式下模块代码打包进 PYZ（嵌入 exe 内），`__file__` 形如 `exe路径/app/gui_qt.pyc`（虚拟 zip 内路径），不是文件系统真实路径。`os.path.dirname(__file__)` 拼接出的资源路径均无效。解决方案：用 `sys._MEIPASS`（onedir 指向 `_internal/`）+ 相对路径定位资源文件。统一封装为 `_resource_dir()` 工具函数。

22. **PyInstaller onedir 中资源文件的实际存储位置**: `datas` 列表中的文件复制到 `_internal/` 下对应子目录（如 `_internal/app/resources/*.svg`），而非 exe 同级。代码中用 `sys._MEIPASS + '/app/resources/'` 定位。注意与 onefile 不同：onefile 用临时目录，onedir 用 exe 同级的 `_internal/`。

23. **第三方包的数据文件需显式收集**: `airportsdata` 包内部读取 `airports.csv` / `iata_macs.csv`，PyInstaller 只打包 `.py` 代码，不会自动包含非代码数据文件。解决方案：spec 中用 `from PyInstaller.utils.hooks import collect_data_files` + `collect_data_files('airportsdata')` 自动收集包内所有数据文件。
